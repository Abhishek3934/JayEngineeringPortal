#spring.application.name=JayEngineeringPortal
#
## --- Database Connection (LOCAL) ---
#spring.datasource.url=jdbc:mysql://localhost:3306/jayeng_db?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=root
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#
## --- Hibernate / JPA ---
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.show-sql=false
#
## --- SQL Initialization ---
#spring.datasource.initialization-mode=never
#
## --- Local Server Port ---
#server.port=8080




spring.application.name=JayEngineeringPortal

# --- Database connection (Clever Cloud) ---
spring.datasource.url=jdbc:mysql://${MYSQL_ADDON_HOST}:${MYSQL_ADDON_PORT}/${MYSQL_ADDON_DB}
spring.datasource.username=${MYSQL_ADDON_USER}
spring.datasource.password=${MYSQL_ADDON_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- Hibernate / JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# --- SQL Initialization ---
spring.sql.init.mode=never

# --- Server Port ---
server.port=${PORT:8080}
